{
  "extends": "../../../tsconfig.json", // Adjust if necessary
  "compilerOptions": {
    "jsx": "react",
    "outDir": "dist",
    "rootDir": "."
  },
  "include": [
    "**/*"
  ],
  "exclude": ["node_modules", "dist", "../../backend"]
}
